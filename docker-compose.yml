version: '3'
services:
  mongodb:
    image: mongo
    container_name: mongodb
    restart: always
    environment:
      MONGO_INITDB_DATABASE: promptgen
    volumes:
      - mongodb_data:/data/db
    ports:
      - "27018:27017"

volumes:
  mongodb_data:
